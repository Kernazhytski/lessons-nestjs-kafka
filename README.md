# Kafka Lesson - Обучающий проект по микросервисной архитектуре

## 📚 Описание проекта

Этот проект создан для изучения микросервисной архитектуры с использованием Apache Kafka. Проект демонстрирует взаимодействие двух NestJS сервисов через Kafka для асинхронной обработки тяжелых вычислений (умножение матриц).

### Цели обучения

- Понимание принципов микросервисной архитектуры
- Работа с Apache Kafka для асинхронной коммуникации между сервисами
- Использование Bull Queue для управления очередями задач
- Разделение ответственности между сервисами
- Обработка больших объемов данных

## 🏗️ Архитектура проекта

Проект состоит из двух микросервисов:

1. **kafka-project1** - API сервис (порт 3000)
   - Принимает HTTP запросы от клиентов
   - Отправляет задачи в Kafka
   - Получает результаты через Kafka
   - Хранит результаты в памяти

2. **kafka-project2** - Сервис вычислений (порт 3001)
   - Получает задачи из Kafka
   - Обрабатывает задачи через Bull Queue (Redis)
   - Выполняет умножение матриц
   - Отправляет результаты обратно в Kafka

### Схема взаимодействия

```
┌─────────────┐
│   Client    │ HTTP POST /matrix/multiply
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────┐
│   kafka-project1 (API Service)      │
│   - Принимает запросы               │
│   - Генерирует UUID                 │
│   - Сохраняет статус "pending"      │
└──────┬──────────────────────────────┘
       │
       │ Kafka Producer
       │ Topic: matrix-multiplication-request
       ▼
┌─────────────────────────────────────┐
│         Apache Kafka Broker          │
│   - Хранит сообщения                │
│   - Гарантирует доставку            │
└──────┬──────────────────────────────┘
       │
       │ Kafka Consumer
       │ Topic: matrix-multiplication-request
       ▼
┌─────────────────────────────────────┐
│  kafka-project2 (Calculator Service) │
│   - Получает задачу из Kafka         │
│   - Добавляет в Bull Queue           │
└──────┬──────────────────────────────┘
       │
       │ Bull Queue (Redis)
       ▼
┌─────────────────────────────────────┐
│      MatrixProcessor (Bull)          │
│   - Обрабатывает задачу             │
│   - Выполняет умножение матриц       │
└──────┬──────────────────────────────┘
       │
       │ Kafka Producer
       │ Topic: matrix-multiplication-response
       ▼
┌─────────────────────────────────────┐
│         Apache Kafka Broker          │
└──────┬──────────────────────────────┘
       │
       │ Kafka Consumer
       │ Topic: matrix-multiplication-response
       ▼
┌─────────────────────────────────────┐
│   kafka-project1 (API Service)      │
│   - Получает результат               │
│   - Обновляет статус на "completed"   │
└──────┬──────────────────────────────┘
       │
       │ HTTP GET /matrix/results/:id
       ▼
┌─────────────┐
│   Client    │
└─────────────┘
```

## 🚀 Быстрый старт

### Требования

- Node.js >= 18.x
- npm >= 9.x
- Docker и Docker Compose
- Git

### Установка и запуск

1. **Клонирование репозитория**
```bash
git clone <repository-url>
cd kafka-lesson
```

2. **Установка зависимостей**
```bash
# Установка зависимостей для первого сервиса
cd kafka-project1
npm install

# Установка зависимостей для второго сервиса
cd ../kafka-project2
npm install
```

3. **Настройка переменных окружения**

Создайте файлы `.env` в каждом проекте:

**kafka-project1/.env:**
```env
PORT=3000
KAFKA_BROKER=localhost:9092
KAFKA_CLIENT_ID=kafka-project1
KAFKA_GROUP_ID=matrix-service-group
KAFKA_REQUEST_TOPIC=matrix-multiplication-request
KAFKA_RESPONSE_TOPIC=matrix-multiplication-response
```

**kafka-project2/.env:**
```env
PORT=3001
KAFKA_BROKER=localhost:9092
KAFKA_CLIENT_ID=kafka-project2
KAFKA_GROUP_ID=matrix-calculator-group
KAFKA_REQUEST_TOPIC=matrix-multiplication-request
KAFKA_RESPONSE_TOPIC=matrix-multiplication-response
REDIS_HOST=localhost
REDIS_PORT=6379
```

4. **Запуск инфраструктуры (Kafka, Redis)**
```bash
# Из корневой директории проекта
docker-compose up -d
```

Проверьте статус контейнеров:
```bash
docker-compose ps
```

5. **Запуск сервисов**

Откройте два терминала:

**Терминал 1 - API Service:**
```bash
cd kafka-project1
npm run start:dev
```

**Терминал 2 - Calculator Service:**
```bash
cd kafka-project2
npm run start:dev
```

6. **Проверка работы**

- API Service: http://localhost:3000/api (Swagger)
- Calculator Service: http://localhost:3001
- Kafka UI: http://localhost:8080

## 📖 Настройка Apache Kafka

### Kafka в режиме KRaft

Проект использует Kafka в режиме KRaft (без Zookeeper), что является современным подходом. Kafka работает как единый брокер с контроллером.

### Конфигурация Docker Compose

В файле `docker-compose.yml` настроен Kafka с использованием переменных окружения. Подробное объяснение каждой переменной:

#### Переменные Kafka (docker-compose.yml)

| Переменная | Описание | Значение по умолчанию |
|-----------|----------|----------------------|
| `KAFKA_PROCESS_ROLES` | Роли процесса Kafka | `broker,controller` |
| `KAFKA_NODE_ID` | Уникальный ID ноды Kafka | `1` |
| `KAFKA_CONTROLLER_QUORUM_VOTERS` | Список контроллеров для кворума | `1@kafka:9093` |
| `KAFKA_LISTENERS` | Список слушателей (портов) | `PLAINTEXT://0.0.0.0:9092,PLAINTEXT_INTERNAL://0.0.0.0:9094,CONTROLLER://0.0.0.0:9093` |
| `KAFKA_ADVERTISED_LISTENERS` | Адреса, которые Kafka сообщает клиентам | `PLAINTEXT://localhost:9092,PLAINTEXT_INTERNAL://kafka:9094` |
| `KAFKA_LISTENER_SECURITY_PROTOCOL_MAP` | Карта протоколов безопасности | `PLAINTEXT:PLAINTEXT,PLAINTEXT_INTERNAL:PLAINTEXT,CONTROLLER:PLAINTEXT` |
| `KAFKA_INTER_BROKER_LISTENER_NAME` | Имя слушателя для связи между брокерами | `PLAINTEXT_INTERNAL` |
| `KAFKA_CONTROLLER_LISTENER_NAMES` | Имена слушателей контроллера | `CONTROLLER` |
| `KAFKA_AUTO_CREATE_TOPICS_ENABLE` | Автоматическое создание топиков | `true` |
| `KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR` | Фактор репликации для топика offsets | `1` |
| `KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR` | Фактор репликации для транзакций | `1` |
| `KAFKA_TRANSACTION_STATE_LOG_MIN_ISR` | Минимальное количество in-sync реплик | `1` |

#### Подробное объяснение переменных

**KAFKA_PROCESS_ROLES=broker,controller**
- Определяет роли процесса Kafka
- `broker` - обрабатывает запросы клиентов
- `controller` - управляет метаданными кластера
- В KRaft режиме один процесс может выполнять обе роли

**KAFKA_NODE_ID=1**
- Уникальный идентификатор ноды в кластере
- Для одного брокера обычно используется `1`

**KAFKA_CONTROLLER_QUORUM_VOTERS=1@kafka:9093**
- Определяет контроллеры для кворума
- Формат: `nodeId@host:port`
- `1@kafka:9093` означает нода с ID=1 на хосте kafka:9093

**KAFKA_LISTENERS**
- Список всех портов, на которых Kafka слушает
- `PLAINTEXT://0.0.0.0:9092` - для внешних клиентов (localhost)
- `PLAINTEXT_INTERNAL://0.0.0.0:9094` - для связи между брокерами (внутри Docker)
- `CONTROLLER://0.0.0.0:9093` - для контроллера

**KAFKA_ADVERTISED_LISTENERS**
- Адреса, которые Kafka сообщает клиентам для подключения
- `PLAINTEXT://localhost:9092` - для внешних клиентов
- `PLAINTEXT_INTERNAL://kafka:9094` - для внутренних сервисов (Kafka UI)

**KAFKA_LISTENER_SECURITY_PROTOCOL_MAP**
- Карта протоколов безопасности для каждого слушателя
- `PLAINTEXT` означает отсутствие шифрования (для разработки)

**KAFKA_INTER_BROKER_LISTENER_NAME=PLAINTEXT_INTERNAL**
- Имя слушателя для связи между брокерами
- Используется для репликации данных

**KAFKA_CONTROLLER_LISTENER_NAMES=CONTROLLER**
- Имена слушателей, используемых контроллером
- В KRaft режиме контроллер управляет метаданными

**KAFKA_AUTO_CREATE_TOPICS_ENABLE=true**
- Автоматически создает топики при первой записи
- Удобно для разработки, но не рекомендуется для продакшена

**KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR=1**
- Количество реплик для топика offsets (отслеживание позиций consumer)
- Для одного брокера достаточно `1`

**KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR=1**
- Количество реплик для транзакционных логов
- Для одного брокера достаточно `1`

**KAFKA_TRANSACTION_STATE_LOG_MIN_ISR=1**
- Минимальное количество in-sync реплик для транзакций
- Для одного брокера достаточно `1`

### Kafka UI

Kafka UI - веб-интерфейс для мониторинга Kafka, доступен на http://localhost:8080

**Переменные Kafka UI:**
- `KAFKA_CLUSTERS_0_NAME` - Имя кластера (по умолчанию `local`)
- `KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS` - Адрес брокера (`kafka:9094` - внутренний адрес)

### Redis

Redis используется для Bull Queue в kafka-project2.

**Переменные Redis:**
- `REDIS_PORT` - Порт Redis (по умолчанию `6379`)
- `command: redis-server --appendonly yes` - Включает персистентность данных

## 📁 Структура проекта

```
kafka-lesson/
├── docker-compose.yml          # Конфигурация инфраструктуры
├── docker-compose.env          # Переменные окружения для Docker
├── README.md                   # Этот файл
├── kafka-project1/             # API сервис
│   ├── src/
│   │   ├── config/            # Конфигурация (Kafka, валидация)
│   │   ├── core/
│   │   │   ├── kafka/         # Kafka producer/consumer
│   │   │   └── matrix/        # Контроллер и сервис для матриц
│   │   └── main.ts
│   ├── package.json
│   └── README.md              # Документация API сервиса
├── kafka-project2/             # Сервис вычислений
│   ├── src/
│   │   ├── config/            # Конфигурация (Kafka, Redis, валидация)
│   │   ├── core/
│   │   │   ├── kafka/         # Kafka producer/consumer
│   │   │   └── matrix/        # Bull Queue processor для расчетов
│   │   └── main.ts
│   ├── package.json
│   └── README.md              # Документация сервиса вычислений
└── matrices-100x100.json      # Пример больших матриц для тестирования
```

## 🔧 Использование API

### Swagger документация

После запуска сервисов, Swagger документация доступна по адресу:
- **API Service:** http://localhost:3000/api

### Примеры запросов

#### 1. Умножение матриц (POST /matrix/multiply)

```bash
curl -X POST http://localhost:3000/matrix/multiply \
  -H "Content-Type: application/json" \
  -d '{
    "matrixA": {
      "data": [[1, 2], [3, 4]]
    },
    "matrixB": {
      "data": [[5, 6], [7, 8]]
    }
  }'
```

**Ответ:**
```json
{
  "id": "123e4567-e89b-12d3-a456-426614174000"
}
```

#### 2. Получить все результаты (GET /matrix/results)

```bash
curl http://localhost:3000/matrix/results
```

#### 3. Получить результат по ID (GET /matrix/results/:id)

```bash
curl http://localhost:3000/matrix/results/123e4567-e89b-12d3-a456-426614174000
```

**Ответ:**
```json
{
  "id": "123e4567-e89b-12d3-a456-426614174000",
  "result": [[19, 22], [43, 50]],
  "status": "completed",
  "createdAt": "2026-01-21T22:00:00.000Z",
  "completedAt": "2026-01-21T22:00:01.000Z"
}
```

### Работа с большими матрицами

Для тестирования с большими матрицами (например, 100x100) используйте файл `matrices-100x100.json`:

```bash
curl -X POST http://localhost:3000/matrix/multiply \
  -H "Content-Type: application/json" \
  -d @matrices-100x100.json
```

**Примечание:** Лимит размера тела запроса увеличен до 10MB для поддержки больших матриц.

## 🔍 Мониторинг

### Kafka UI

Веб-интерфейс для мониторинга Kafka доступен по адресу: http://localhost:8080

Здесь вы можете:
- Просматривать топики и сообщения
- Мониторить consumer groups
- Управлять топиками
- Просматривать метрики

### Логи

Логи сервисов выводятся в консоль. Для просмотра логов Docker контейнеров:

```bash
# Все контейнеры
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f kafka
docker-compose logs -f redis
docker-compose logs -f kafka-ui
```

## 🐛 Решение проблем

### Kafka не запускается

```bash
# Проверьте статус контейнеров
docker-compose ps

# Перезапустите контейнеры
docker-compose restart

# Просмотрите логи
docker-compose logs kafka
```

### Ошибки подключения к Kafka

Убедитесь, что:
1. Kafka контейнер запущен: `docker-compose ps`
2. Порт 9092 доступен
3. Переменная `KAFKA_BROKER` в `.env` правильная (`localhost:9092`)

### Ошибки подключения к Redis

Убедитесь, что:
1. Redis контейнер запущен: `docker-compose ps`
2. Порт 6379 доступен
3. Переменные `REDIS_HOST` и `REDIS_PORT` в `.env` правильные

### PayloadTooLargeError

Если получаете ошибку "request entity too large", убедитесь, что в `kafka-project1/src/main.ts` настроен увеличенный лимит:

```typescript
app.use(express.json({ limit: '10mb' }));
```

## 📚 Дополнительные ресурсы

- [Документация Apache Kafka](https://kafka.apache.org/documentation/)
- [Документация NestJS](https://docs.nestjs.com/)
- [Документация Bull Queue](https://github.com/OptimalBits/bull)
- [Документация KafkaJS](https://kafka.js.org/)

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 👥 Авторы

Учебный проект для изучения микросервисной архитектуры и Apache Kafka.
