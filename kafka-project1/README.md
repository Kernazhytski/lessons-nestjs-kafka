# kafka-project1 - API Service

## 📋 Описание

**kafka-project1** - это основной API сервис, который принимает HTTP запросы от клиентов для умножения матриц. Сервис работает асинхронно: принимает запрос, отправляет его в Kafka, и затем получает результат обратно через Kafka.

## 🎯 Роль в архитектуре

Этот сервис является **точкой входа** (entry point) для клиентов. Он:

1. **Принимает HTTP запросы** от клиентов с матрицами для умножения
2. **Генерирует уникальный ID** для каждого запроса (UUID)
3. **Отправляет задачу в Kafka** в топик `matrix-multiplication-request`
4. **Сохраняет статус** запроса в памяти (pending/completed/error)
5. **Получает результаты** из Kafka из топика `matrix-multiplication-response`
6. **Предоставляет API** для получения результатов по ID

## 🏗️ Архитектура сервиса

```
┌─────────────────────────────────────────┐
│         HTTP Client                      │
└──────────────┬──────────────────────────┘
               │
               │ POST /matrix/multiply
               ▼
┌─────────────────────────────────────────┐
│      MatrixController                   │
│   - Валидация запроса                    │
│   - Вызов MatrixService                  │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│      MatrixService                       │
│   - Генерация UUID                       │
│   - Сохранение статуса "pending"         │
│   - Отправка в Kafka Producer            │
└──────────────┬──────────────────────────┘
               │
               │ Kafka Producer
               │ Topic: matrix-multiplication-request
               ▼
┌─────────────────────────────────────────┐
│         Apache Kafka                    │
└─────────────────────────────────────────┘
               │
               │ Kafka Consumer
               │ Topic: matrix-multiplication-response
               ▼
┌─────────────────────────────────────────┐
│      KafkaConsumerService                │
│   - Получение результата из Kafka        │
│   - Вызов MatrixService.handleResponse() │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│      MatrixService                       │
│   - Обновление статуса на "completed"    │
│   - Сохранение результата                │
└─────────────────────────────────────────┘
               │
               │ GET /matrix/results/:id
               ▼
┌─────────────────────────────────────────┐
│         HTTP Client                      │
└─────────────────────────────────────────┘
```

## 📁 Структура проекта

```
kafka-project1/
├── src/
│   ├── main.ts                          # Точка входа приложения
│   ├── app.module.ts                    # Корневой модуль
│   ├── config/
│   │   ├── config.module.ts            # Модуль конфигурации
│   │   ├── config.validation.ts        # Схема валидации env переменных
│   │   └── kafka.config.ts             # Конфигурация Kafka
│   └── core/
│       ├── kafka/
│       │   ├── kafka.module.ts          # Модуль Kafka
│       │   ├── kafka-producer.service.ts # Отправка сообщений в Kafka
│       │   └── kafka-consumer.service.ts # Получение сообщений из Kafka
│       └── matrix/
│           ├── matrix.module.ts         # Модуль матриц
│           ├── matrix.controller.ts    # HTTP контроллер
│           ├── matrix.service.ts        # Бизнес-логика
│           └── dto/
│               ├── matrix-multiplication.dto.ts      # DTO для запроса
│               ├── matrix-request-response.dto.ts   # DTO для ответа
│               └── matrix-results-list.dto.ts        # DTO для списка результатов
├── package.json
└── README.md
```

## 🔧 Компоненты сервиса

### 1. MatrixController

**Файл:** `src/core/matrix/matrix.controller.ts`

HTTP контроллер, который обрабатывает запросы от клиентов.

**Эндпоинты:**
- `POST /matrix/multiply` - Отправить запрос на умножение матриц
- `GET /matrix/results` - Получить все результаты
- `GET /matrix/results/:id` - Получить результат по ID

**Особенности:**
- Использует `@HttpCode(HttpStatus.ACCEPTED)` для POST запроса (202 Accepted)
- Валидация через `ValidationPipe` и DTO
- Swagger документация через `@ApiTags`, `@ApiOperation`, `@ApiResponse`

### 2. MatrixService

**Файл:** `src/core/matrix/matrix.service.ts`

Сервис, который управляет жизненным циклом запросов на умножение матриц.

**Основные методы:**

- `multiplyMatrices(matrixA, matrixB)` - Создает новый запрос
  - Генерирует UUID для запроса
  - Сохраняет статус "pending" в памяти
  - Отправляет задачу в Kafka через `KafkaProducerService`
  - Возвращает ID запроса

- `handleResponse(response)` - Обрабатывает результат из Kafka
  - Обновляет статус на "completed" или "error"
  - Сохраняет результат матрицы
  - Устанавливает время завершения

- `getAllResults()` - Возвращает все результаты (сортировка по дате)
- `getResultById(id)` - Возвращает результат по ID

**Хранение данных:**
- Использует `Map<string, MatrixResult>` для хранения результатов в памяти
- **Важно:** Данные теряются при перезапуске сервиса (для продакшена нужна БД)

**Интерфейс MatrixResult:**
```typescript
interface MatrixResult {
  id: string;
  result?: number[][];
  status: 'pending' | 'completed' | 'error';
  error?: string;
  createdAt: Date;
  completedAt?: Date;
}
```

### 3. KafkaProducerService

**Файл:** `src/core/kafka/kafka-producer.service.ts`

Сервис для отправки сообщений в Kafka.

**Основные методы:**
- `sendMessage(topic, message)` - Отправляет сообщение в указанный топик
  - Сериализует сообщение в JSON
  - Использует ID сообщения как ключ (для партиционирования)
  - Логирует отправку

**Особенности:**
- Автоматическое переподключение при ошибках (retry механизм)
- Использует `LegacyPartitioner` для партиционирования
- Подключается при инициализации модуля (`OnModuleInit`)
- Отключается при завершении работы (`OnModuleDestroy`)

### 4. KafkaConsumerService

**Файл:** `src/core/kafka/kafka-consumer.service.ts`

Сервис для получения сообщений из Kafka.

**Основные методы:**
- `subscribe(topic, callback)` - Подписывается на топик
  - Читает сообщения с начала топика (`fromBeginning: true`)
  - Парсит JSON сообщения
  - Вызывает callback для каждого сообщения
  - Обрабатывает ошибки парсинга

**Особенности:**
- Использует Consumer Group для балансировки нагрузки
- Автоматическое переподключение при ошибках
- Логирование всех полученных сообщений

### 5. Main.ts

**Файл:** `src/main.ts`

Точка входа приложения. Настраивает:

1. **Body Parser** - Увеличенный лимит до 10MB для больших матриц
   ```typescript
   app.use(express.json({ limit: '10mb' }));
   ```

2. **Validation Pipe** - Глобальная валидация запросов
   ```typescript
   app.useGlobalPipes(new ValidationPipe({
     whitelist: true,
     forbidNonWhitelisted: true,
     transform: true,
   }));
   ```

3. **Swagger** - API документация на `/api`

4. **Kafka Consumer** - Подписка на топик ответов
   ```typescript
   await kafkaConsumer.subscribe(responseTopic, (message) => {
     matrixService.handleResponse(message);
   });
   ```

5. **Задержка инициализации** - 3 секунды для полной инициализации Kafka

## ⚙️ Конфигурация

### Переменные окружения (.env)

| Переменная | Описание | Значение по умолчанию |
|-----------|----------|----------------------|
| `PORT` | Порт HTTP сервера | `3000` |
| `KAFKA_BROKER` | Адрес Kafka брокера | `localhost:9092` |
| `KAFKA_CLIENT_ID` | ID клиента Kafka | `kafka-project1` |
| `KAFKA_GROUP_ID` | ID группы consumer | `matrix-service-group` |
| `KAFKA_REQUEST_TOPIC` | Топик для отправки запросов | `matrix-multiplication-request` |
| `KAFKA_RESPONSE_TOPIC` | Топик для получения ответов | `matrix-multiplication-response` |

### Пример .env файла

```env
PORT=3000
KAFKA_BROKER=localhost:9092
KAFKA_CLIENT_ID=kafka-project1
KAFKA_GROUP_ID=matrix-service-group
KAFKA_REQUEST_TOPIC=matrix-multiplication-request
KAFKA_RESPONSE_TOPIC=matrix-multiplication-response
```

## 🔄 Поток обработки запроса

### 1. Клиент отправляет запрос

```bash
POST /matrix/multiply
{
  "matrixA": { "data": [[1, 2], [3, 4]] },
  "matrixB": { "data": [[5, 6], [7, 8]] }
}
```

### 2. MatrixController получает запрос

- Валидирует данные через DTO
- Вызывает `MatrixService.multiplyMatrices()`

### 3. MatrixService обрабатывает запрос

- Генерирует UUID: `"abc-123-def-456"`
- Сохраняет в памяти:
  ```typescript
  {
    id: "abc-123-def-456",
    status: "pending",
    createdAt: new Date()
  }
  ```
- Отправляет в Kafka через `KafkaProducerService`:
  ```json
  {
    "id": "abc-123-def-456",
    "matrixA": { "data": [[1, 2], [3, 4]] },
    "matrixB": { "data": [[5, 6], [7, 8]] }
  }
  ```

### 4. Возврат ответа клиенту

```json
{
  "id": "abc-123-def-456"
}
```

Статус HTTP: `202 Accepted` (запрос принят, обработка асинхронная)

### 5. Получение результата из Kafka

Когда kafka-project2 завершает вычисления, он отправляет результат в топик `matrix-multiplication-response`:

```json
{
  "id": "abc-123-def-456",
  "result": [[19, 22], [43, 50]],
  "status": "completed"
}
```

### 6. KafkaConsumerService получает результат

- Парсит JSON сообщение
- Вызывает `MatrixService.handleResponse()`

### 7. MatrixService обновляет результат

- Обновляет запись в памяти:
  ```typescript
  {
    id: "abc-123-def-456",
    result: [[19, 22], [43, 50]],
    status: "completed",
    createdAt: Date,
    completedAt: Date
  }
  ```

### 8. Клиент получает результат

```bash
GET /matrix/results/abc-123-def-456
```

```json
{
  "id": "abc-123-def-456",
  "result": [[19, 22], [43, 50]],
  "status": "completed",
  "createdAt": "2026-01-21T22:00:00.000Z",
  "completedAt": "2026-01-21T22:00:01.000Z"
}
```

## 📊 DTO (Data Transfer Objects)

### MatrixMultiplicationRequestDto

```typescript
{
  matrixA: {
    data: number[][]  // Двумерный массив чисел
  },
  matrixB: {
    data: number[][]  // Двумерный массив чисел
  }
}
```

### MatrixMultiplicationRequestResponseDto

```typescript
{
  id: string  // UUID запроса
}
```

### MatrixResultItemDto

```typescript
{
  id: string;
  result?: number[][];
  status: 'pending' | 'completed' | 'error';
  error?: string;
  createdAt: Date;
  completedAt?: Date;
}
```

## 🚀 Запуск сервиса

### Разработка

```bash
npm run start:dev
```

Сервис будет доступен на http://localhost:3000
Swagger документация: http://localhost:3000/api

### Продакшн

```bash
npm run build
npm run start:prod
```

## 🧪 Тестирование

### Unit тесты

```bash
npm run test
```

### E2E тесты

```bash
npm run test:e2e
```

### Покрытие кода

```bash
npm run test:cov
```

## 🔍 Мониторинг и отладка

### Логи

Сервис использует встроенный Logger NestJS. Все важные события логируются:

- Получение HTTP запросов
- Отправка сообщений в Kafka
- Получение сообщений из Kafka
- Обновление статусов результатов
- Ошибки обработки

### Swagger UI

Интерактивная документация API доступна на http://localhost:3000/api

Здесь можно:
- Просмотреть все эндпоинты
- Протестировать API прямо в браузере
- Увидеть схемы данных

## ⚠️ Важные замечания

1. **Хранение в памяти**: Результаты хранятся в памяти и теряются при перезапуске. Для продакшена нужна база данных (PostgreSQL, MongoDB и т.д.)

2. **Нет персистентности**: Если сервис перезапустится до получения результата, результат будет потерян

3. **Лимит размера запроса**: Увеличен до 10MB для поддержки больших матриц (100x100 и больше)

4. **Асинхронность**: Клиент должен опрашивать статус результата через GET запросы, так как обработка асинхронная

5. **Consumer Group**: Используется для балансировки нагрузки. Если запустить несколько инстансов сервиса, они будут распределять сообщения между собой

## 🔗 Связанные компоненты

- **kafka-project2** - Сервис вычислений, который обрабатывает задачи
- **Apache Kafka** - Брокер сообщений
- **Swagger** - API документация

## 📚 Дополнительные ресурсы

- [Документация NestJS](https://docs.nestjs.com/)
- [Документация KafkaJS](https://kafka.js.org/)
- [Документация Swagger для NestJS](https://docs.nestjs.com/openapi/introduction)
